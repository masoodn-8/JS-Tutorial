//for, for of, for in, for each

let nums = [1, 2, 3, 5, 9, 10];

const map = new Map();
map.set("Me" , "Masood");
map.set("Mine", "Athira");
map.set("Mine", "Asmitha");

for (const [key, value] of map) {
    console.log(key, value);
    
}

// for (let i = 0; i < nums.length; i++) {
//     console.log(nums[i]);
    
    
// }

